<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="velsol.in">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.employee.InviteeArrivalActivity"></activity>
        <activity android:name=".activities.employee.EmployeeEmailVerificationActivity" />
        <activity android:name=".activities.employee.EmployeeCreatePasswordActivity" />
        <activity android:name=".activities.employee.EmployeeAuthorizationActivity" />
        <activity android:name=".activities.employee.EmployeeForgotPasswordActivity" />
        <activity
            android:name=".activities.employee.EmployeeLoginActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.employee.AddInviteeAppointmentActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.employee.EmployeeHomeActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.WelcomeActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.IntroActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>